# <Title goes here — verb + object + scope>

<!--
Pre-flight checklist before filing:
- [ ] Title is one line, scoped to what changes (not "improve perf" — "stream CSV from /reports/export").
- [ ] Acceptance criteria are checkable (run X, observe Y).
- [ ] You've checked: is there an existing ticket / wiki page / utility for this?
-->

## Context
Why are we doing this? Who asked? What does the user see today vs. after?

## Acceptance criteria
- [ ] Bullet 1 (observable from the outside)
- [ ] Bullet 2
- [ ] Bullet 3 (include any latency / size threshold; "fast" doesn't count)

## Out of scope
- Thing-A — separate ticket
- Thing-B — different team owns it

## Constraints / hints (optional)
- Touch only `<path>`
- Reuse `<existing util>`; don't add a new HTTP client
- Match the pattern in `<reference file>`
